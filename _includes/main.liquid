<!doctype html>
<html lang="en">
    <head>
        {% include head.liquid %}
    </head>
    <body class="{{ bodyClass }}">
        <main class="article">
        <h1>{{ pageTitle }}</h1>
        {{ content }}
        </main>
    </body>
</html>