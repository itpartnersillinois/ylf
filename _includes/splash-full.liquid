<!doctype html>
<html lang="en">
    <head>
        {% include head.liquid %}
    </head>
    <body>
        <main class="splash" style="background-image: url(../img/{{ backgroundImage }});">
        <div class="full">
        <h1 class="intro-text">{{ pageTitle }} </h1>
        <ul class="stats no-bullets">
        {% for stat in stats %}
            <li>{{ stat | transformStat }}</li>
        {% endfor %}
        </ul>
        </div>
        </main>
    </body>
</html>